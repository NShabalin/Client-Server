@startuml ProgramFlow
start
:Запуск программы;
:Выбор между сервером и клиентом;
if (Выбран клиент?) then (да)
  :Запуск клиента;
  :Подключение к серверу;
  while (Пользователь выбрал "Выход"?) is (нет)
    :Отправление команды серверу;
    :Получение ответа от сервера;
  endwhile
  :Завершение клиента;
  stop
else (нет)
  :Инициализация сервера;
  while (Пользователь ввел "exit"?) is (нет)
    :Ожидание команд от клиентов;
    :Обработка команд, высылание результатов;
  endwhile
  :Завершение сервера;
  stop
endif
@enduml
